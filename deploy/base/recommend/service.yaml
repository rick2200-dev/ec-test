apiVersion: v1
kind: Service
metadata:
  name: recommend
  labels:
    app.kubernetes.io/name: recommend
    app.kubernetes.io/component: service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8086
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: recommend
