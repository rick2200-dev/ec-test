// ProductForm is a controlled (pure) component, so the Presenter is the component itself.
// The previous public name `ProductForm` is preserved as an alias.
export {
  ProductFormPresenter as ProductForm,
  ProductFormPresenter,
  type ProductFormPresenterProps,
  type ProductFormPresenterProps as ProductFormProps,
} from "./ProductForm.presenter";
