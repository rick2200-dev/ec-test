// StatsCard is a pure presentational component, so the Presenter is the default export.
import { StatsCardPresenter } from "./StatsCard.presenter";

export default StatsCardPresenter;
export { StatsCardPresenter, type StatsCardPresenterProps } from "./StatsCard.presenter";
