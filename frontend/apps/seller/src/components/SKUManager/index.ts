// SKUManager is a controlled (pure) component, so the Presenter is the component itself.
export {
  SKUManagerPresenter as SKUManager,
  SKUManagerPresenter,
  type SKUManagerPresenterProps,
  type SKUInput,
} from "./SKUManager.presenter";
