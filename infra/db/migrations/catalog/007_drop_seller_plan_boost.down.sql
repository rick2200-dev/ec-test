-- Recreating the matview exactly as 004 had it would require re-reading
-- that migration's body; for a rollback in a non-production setting,
-- re-run the 004 up.sql manually or recreate from a known-good backup.
-- Left as a no-op to keep this file idempotent.
SELECT 1;
