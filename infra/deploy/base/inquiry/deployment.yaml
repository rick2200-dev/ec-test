apiVersion: apps/v1
kind: Deployment
metadata:
  name: inquiry
  labels:
    app.kubernetes.io/name: inquiry
    app.kubernetes.io/component: service
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: inquiry
  template:
    metadata:
      labels:
        app.kubernetes.io/name: inquiry
        app.kubernetes.io/component: service
    spec:
      serviceAccountName: inquiry
      terminationGracePeriodSeconds: 30
      containers:
        - name: inquiry
          image: inquiry
          ports:
            - name: http
              containerPort: 8090
              protocol: TCP
          envFrom:
            - secretRef:
                name: inquiry-secrets
            - configMapRef:
                name: inquiry-config
          env:
            - name: HTTP_PORT
              value: "8090"
            - name: LOG_LEVEL
              value: "info"
            - name: LOG_FORMAT
              value: "json"
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
